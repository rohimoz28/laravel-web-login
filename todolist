
    ## Change Route ## 
    ## Auth Route ##

1. Auth Route
  - login => '/'
  - logout => '/logout'

2. User Route
  - dashboard => '/user'

    ## Change Middleware ## 
1. OnlyMember Middleware | ($cookie) : $next ? back to '/'
  - '/user'
  - '/logout'

2. OnlyGuest Middleware | ($cookie) : back to '/user' ? $next
    - '/'
    - '/register'
    - '/forgot'
